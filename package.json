{
  "name": "discord-apex-assistant",
  "version": "1.0.0",
  "description": "discord bot apex legends utilities for private server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/server.js",
    "dev": "ts-node-dev src/server.ts",
    "clean": "rimraf ./build",
    "build": "tsc"
  },
  "author": "A.Netiphong",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "discord.js": "^13.7.0",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "eslint": "^8.16.0",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^2.0.0"
  }
}
